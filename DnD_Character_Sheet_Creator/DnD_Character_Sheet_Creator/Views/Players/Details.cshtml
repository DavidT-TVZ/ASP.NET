@model DnD_Character_Sheet_Creator.Models.Player
@using DnD_Character_Sheet_Creator.Models

@{
    ViewData["Title"] = "Player Details";
    var characters = ViewBag.Characters as IEnumerable<Character> ?? Enumerable.Empty<Character>();
}

<section class="sheet-card mb-4 reveal">
    <p class="sheet-kicker mb-1">Guild Member Dossier</p>
    <h1 class="h3 mb-0">Player Details</h1>
</section>

<div class="mb-4">
    <a class="btn btn-outline-ink" asp-controller="Players" asp-action="Index">Back to all players</a>
</div>

<div class="card sheet-card mb-4 reveal">
    <div class="card-body">
        <dl class="row mb-0">
            <dt class="col-sm-3">Name</dt>
            <dd class="col-sm-9">@Model.Name @Model.Surname</dd>

            <dt class="col-sm-3">Username</dt>
            <dd class="col-sm-9">@Model.Username</dd>

            <dt class="col-sm-3">Email</dt>
            <dd class="col-sm-9">@Model.Email</dd>

            <dt class="col-sm-3">Last Login</dt>
            <dd class="col-sm-9"><time datetime="@Model.LastLogin.ToString("o")">@Model.LastLogin.ToString("u")</time></dd>
        </dl>
    </div>
</div>

<h2 class="h4 mb-3">Characters</h2>
@if (!characters.Any())
{
    <div class="alert parchment-alert" role="status">This player has no characters in the mock repository.</div>
}
else
{
    <div class="row g-4">
        @foreach (var character in characters)
        {
            <div class="col-12 col-lg-6">
                <div class="card sheet-card card-hover h-100 reveal">
                    <div class="card-body">
                        <h3 class="h5 card-title">@character.CharacterName</h3>
                        <p class="card-text mb-1"><strong>Class:</strong> @character.Class</p>
                        <p class="card-text mb-1"><strong>Level:</strong> @character.Level.Level</p>
                        <p class="card-text mb-1"><strong>Race:</strong> @character.Race</p>
                        <p class="card-text mb-0"><strong>Alignment:</strong> @character.Alignment</p>
                    </div>
                    <div class="card-footer bg-transparent border-top-0">
                        <a class="btn btn-outline-ink btn-sm" asp-controller="Characters" asp-action="Details" asp-route-id="@character.CharacterId">View details</a>
                    </div>
                </div>
            </div>
        }
    </div>
}
